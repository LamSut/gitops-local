apiVersion: v1
kind: Service
metadata:
  name: {{ include "pizza-fe.fullname" . }}
  labels:
    {{- include "pizza-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pizza-fe.selectorLabels" . | nindent 4 }}
