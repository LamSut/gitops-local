apiVersion: v1
kind: Service
metadata:
  name: {{ include "pizza-be.fullname" . }}
  labels:
    {{- include "pizza-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pizza-be.selectorLabels" . | nindent 4 }}
