apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pizza-be.fullname" . }}-test-connection"
  labels:
    {{- include "pizza-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pizza-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
